apiVersion: apps/v1beta1
kind: StatefulSet
metadata:
  name: sf-redis-master
spec:
  serviceName: "svc-redis-master"
  replicas: 1
  template:
    metadata:
      labels:
        name: sf-redis-master
        environment: test
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: cont-redis-master
        image: h3crd-wlan1.chinacloudapp.cn:5000/redis:v3
        env:
        - name: MASTER
          value: "true"
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: redis-m-p-storage
          mountPath: /data/redis/master
  volumeClaimTemplates:
  - metadata:
      name: redis-m-p-storage
      annotations:
        volume.beta.kubernetes.io/storage-class: "fast"
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 8Gi
